<local:BasePage
	x:Class="FamilyLifeTree.UWP.Views.Pages.TreePage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="using:FamilyLifeTree.UWP.Views.Pages"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:pageVM="using:FamilyLifeTree.ViewModels.Pages"
	d:DataContext="{d:DesignInstance pageVM:TreePageViewModel}"
	mc:Ignorable="d">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="AUTO" />
			<RowDefinition />
		</Grid.RowDefinitions>

		<Button />

		<ScrollViewer
			Grid.Row="1"
			HorizontalScrollBarVisibility="Hidden"
			MaxZoomFactor="10"
			MinZoomFactor="0.1"
			VerticalScrollBarVisibility="Hidden"
			ZoomMode="Enabled">
			<ItemsControl>
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<Canvas />
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>

				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<Grid
							Width="50"
							Height="50"
							Background="Blue" />
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</ScrollViewer>
	</Grid>
</local:BasePage>
